# Scope Document — {Tên Tính Năng}

> **Date**: {YYYY-MM-DD}
> **Status**: Initial | Updated | Ready
> **Skill**: context-before-fix v1.1.0

---

## §1: Problem Summary

{Mô tả ngắn gọn vấn đề được báo cáo}

**Entry Point**: {file/component}
**Feature Area**: {module/feature name}

---

## §2: Codegraph & Scoping Status

- **Codegraph Used**: {true | false}
- **Status**: {active | timeout | error}
- **Stale Index Detected**: {true | false} (Nếu true, ghi nhận cảnh báo stale index tại §9 Confidence Assessment và in log ra console)

---

## §3: Scope Definition

### 3.1 Problem Area

- **Module**: {module name}
- **Component**: {component/file}
- **Function**: {function name}

### 3.2 Boundary

**In Scope**:
- {list of in-scope items}

**Out of Scope**:
- {list of out-of-scope items}

---

## §4: Impact Analysis

### 4.1 Direct Impact

| File | Line(s) | Issue |
|------|---------|-------|
| | | |

### 4.2 Indirect Impact

| File | Relationship | Description |
|------|--------------|-------------|
| | | |

### 4.3 API Contracts Affected

| Endpoint/Interface | Status | Notes |
|-------------------|--------|-------|
| | | |

---

## §5: Call Chain

```mermaid
graph TD
    %% Sơ đồ mẫu truy vết call chain từ Codegraph AST hoặc Grep Fallback
    A["Entry Point (Symbol)"] -->|Calls / Downstream| B["Callee Function 1"]
    B -->|Calls| C["Callee Function 2"]
    D["Upstream Caller Function"] -->|Calls / Upstream| A
```

### Callers (Upstream)
- `{file}:{function}` — gọi entry point (truy vết bằng codegraph_callers hoặc grep_search)
- ...

### Callees (Downstream)
- `{file}:{function}` — được entry point gọi (truy vết bằng codegraph_callees hoặc view_file)
- ...

---

## §6: Data Flow

### 6.1 Input
- {data/input 1}
- {data/input 2}

### 6.2 Output
- {data/output 1}
- {data/output 2}

### 6.3 Dependencies
- {dependency 1}
- {dependency 2}

---

## §7: Affected Components

### 7.1 Files
```
• {file1}
• {file2}
• {file3}
```

### 7.2 Functions/APIs
```
• {file1}:{function1}()
• {file2}:{function2}()
• {file3}:{apiEndpoint}
```

### 7.3 Shared Dependencies
```
• {shared util 1}
• {shared util 2}
```

---

## §8: Evidence

<evidence>
<file>{file path}</file>
<line>{line number}</line>
<method>{codegraph_ast | classic_grep}</method>
<finding>{specific finding & result of cross-verification using view_file}</finding>
</evidence>

<evidence>
<file>{file path}</file>
<line>{line number}</line>
<method>{codegraph_ast | classic_grep}</method>
<finding>{specific finding & result of cross-verification using view_file}</finding>
</evidence>

---

## §9: Confidence Assessment

```yaml
overall_confidence: {X}%

breakdown:
  entry_point_identification: {X}%
  impact_mapping: {X}%
  call_chain_trace: {X}%
  evidence_verification: {X}%

codegraph_consistency:
  check_status: {Passed | Failed | Stale Index Warnings Logged}
  stale_nodes: [] # Danh sách các files/symbols bị lệch so với ổ đĩa thực tế nếu có
```

### Uncertainty Flags / Stale Index Warnings
- {flag 1 — what you're unsure about}
- {WARNING: Codegraph index is stale for symbol X in file Y - ghi nhận tại đây nếu phát hiện lệch khi verify bằng view_file}

---

## §10: Open Questions

| # | Question | Priority | Status |
|---|----------|----------|--------|
| 1 | | | Open/Resolved |
| 2 | | | Open/Resolved |

---

## §11: Next Steps

{Instructions for the next phase — what should happen AFTER this context document}

```
✓ Scope Context Document Complete
✓ NO Code Changes Made
✓ Document ready for fix phase
```

---

> **Document**: `docs/context-to-work/{feature-name}/scope.{YYYY-MM-DD}.md`
> **Generated by**: context-before-fix v1.1.0
> **Language**: Vietnamese

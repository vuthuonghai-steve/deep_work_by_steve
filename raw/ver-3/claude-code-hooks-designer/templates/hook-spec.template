# Hook Design Spec
# Generated by claude-code-hooks-designer. [TỪ DESIGN §6 IP#1]
# Trace: intent -> event -> matcher -> handler -> if-condition

## Intent
{{intent_summary}}

## Event
- name: {{event_name}}
- phase: {{event_phase}}
- matcher_support: {{matcher_support}}
- blocking_capable: {{blocking_capable}}

## Matcher
- raw: "{{matcher_raw}}"
- classified_mode: {{matcher_mode}}   # exact_or | regex
- footgun_warning: {{footgun_warning}}  # e.g. "contains '.' -> forced regex" or "none"

## Handler
- type: {{handler_type}}   # command | prompt | agent
- blocking_format: {{blocking_format}}   # A | B | none (A/B MUST NOT mix)
- timeout: {{timeout_seconds}}
- if_condition: {{if_condition}}   # e.g. tool.name == "bash" or empty
- continueOnBlock: {{continueOnBlock}}   # bool; only valid Stop/SubagentStop
- description: {{handler_description}}

## Target Location
- key: {{target_location_key}}   # user|project|local|plugin|agent_frontmatter
- file: {{target_file_path}}
- shadowing_warning: {{shadowing_warning}}   # empty or warning text

## Verification Plan
- trigger: {{verify_trigger_description}}
- expected: {{verify_expected_behavior}}   # block | allow | audit
